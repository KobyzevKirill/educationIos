Дан список физических объектов, на которые пользователю необходимо выехать для проведения работы. По каждому объекту есть свой список задач.
Реализовать экран, отображающий список объектов (см. макет drop_down_list_1). По тапу на объект, список расширяется и отображается список задач, соответствующих выбранному объекту (см. макет drop_down_list_2). Закрыть список задач объекта можно только по тапу на кнопку "^". Одновременно можно открывать списки задач разных объектов.
По тапу на область с контактным именем и номером телефона выполнять вызов по соответствующему номеру.
По тапу на область кнопки "На карте" открыть стандартную карту, установив на ней ней маркер по координатам, полученным с контактной информацией.

Каркас приложения и базовые классы не добавлены, необходимо это сделать самому. Так же требуется добавить и установить необходимые зависимости с Podfile файл.

Поведение следующее:
Пользователь запускает приложение. Открывается LaunchScreen, затем разрабатываемый экран. Сразу после открытия, на экране необходимо отобразить закешированный список данных. После этого, выполнить запрос к серверу (симуляция), сохранить в БД полученные данные (закешировать) и отобразить новый список на экране.
