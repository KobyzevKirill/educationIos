Завершающий этап в части изучения основ Rx. Содержит одну большую задачу, связанную с определением геолокации. Требуется реализовать сервис, отвечающий за работу с геолокацией (задание детально описано внутри сервиса). Сервис используется на двух экранах (как именно описано внутри соответствующего экрана). Так же потребуется реализовать переходы на экраны в SelectTaskViewController.
